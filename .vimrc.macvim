"------------------------------------------------
" MacVim用の設定
"------------------------------------------------
set noundofile " MacVimのundoファイルを作らない

" MacVimを起動時にフルスクリーンに
" if has("gui_running")
"   set fuoptions=maxvert,maxhorz
"   au GUIEnter * set fullscreen
" endif




